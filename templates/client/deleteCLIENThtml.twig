<form method="post" action="{{ path('app_car_delete', {'id_car': car.id_car}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id_car) }}">
    <button class="btn">Delete</button>
</form>
