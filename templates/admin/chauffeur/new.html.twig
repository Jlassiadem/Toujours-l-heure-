{% extends 'base.html.twig' %}

{% block title %}New Chauffeur{% endblock %}

{% block body %}
    <h1>Create new Chauffeur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_chauffeur_indexad') }}">back to list</a>
{% endblock %}