<form method="post" action="{{ path('app_chauffeur_deletead', {'chauffeur_id': chauffeur.chauffeur_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.chauffeur_id) }}">
    <button class="btn">Delete</button>
</form>